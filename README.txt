WIP Scanner - versión conectada a Google Apps Script.

Esta versión usa una solicitud POST simple en modo no-cors para evitar el bloqueo CORS del navegador al comunicarse con Google Apps Script.

URL de recepción predeterminada: la implementación de Google Apps Script configurada por el usuario.

Después de enviar, verificar la hoja REGISTRO WIP.
